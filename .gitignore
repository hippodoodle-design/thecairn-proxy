node_modules
.env
.env.local
.env.*.local
.env.smoke
# Wave 5c — never commit the plain-text bootstrap bearer.
.buddy-bootstrap-token.tmp
*.tmp
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
.vscode
.idea
dist
build
coverage
*.log

# Claude Code per-machine local config (not for the remote)
.claude/
